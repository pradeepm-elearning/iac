# ANSIBLE
### CHEAT SHEETS
#### ANSIBLE DOCUMENTS
`ansible-doc -l` </br>
`ansible-doc <<MODULE_NAME>>` </br>
`ansible-doc -t inventory -l` </br>

#### ANSIBLE | MODULES
| MODULES | DESCRIPTION |
|-|-|
| `ping` | |
| `shell` | |
| `copy` | |
| `file` | |
| `` | |
| `` | |
| `` | |


#### ANSIBLE COMMANDS
| DESCRIPTION | COMMANDS |
|-|-|
| Ansible syntax verification | `ansible-playbook <<FILENAME>> --syntax-check` |
| Ansible simulate the execution (dry run) | `ansible-playbook <<FILENAME>> --check` |
| Ansible simulate the execution (dry run) | `ansible-playbook <<FILENAME>> --check --diff` |
| Ansible Verbose | `-v` or `-vv` or `-vvv` and so on |
